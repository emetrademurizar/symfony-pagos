services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: symfony-pagos:prod
    ports:
      - "${APP_PORT:-8000}:${APP_PORT:-8000}"
    env_file:
      - .env
    environment:
      APP_ENV: prod
      APP_DEBUG: "0"
      APP_PORT: ${APP_PORT:-8000}
    volumes:
      - jwt_keys:/app/config/jwt
      - app_var:/app/var
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-fsS",
          "http://127.0.0.1:${APP_PORT:-8000}/",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

volumes:
  jwt_keys:
  app_var:
